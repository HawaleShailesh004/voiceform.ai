# FormBot Backend Dependencies

# API Framework
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9
pydantic>=2.7.0,<3

# AI APIs
anthropic==0.40.0
openai==1.54.0

# PDF Processing
PyMuPDF==1.24.11        # fitz — AcroForm reading, PDF rendering, fill-back
pypdf==4.3.1            # Backup PDF tools
reportlab==4.2.2        # PDF generation if needed

# Image Processing  
Pillow==10.4.0          # Image manipulation for overlay fill-back

# Environment & Utils
python-dotenv==1.0.1
httpx==0.27.2           # Async HTTP (for Twilio, webhooks)

# Session Store (choose one in prod)
# redis==5.1.1            # Uncomment for Redis
# supabase==2.9.1         # Uncomment for Supabase

# Optional: Voice
# deepgram-sdk==3.7.0     # Uncomment for Deepgram STT
